on: push
name: 🚀 Deploy no push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Pegar o código
      uses: actions/checkout@v3
    
    - name: 📂 Sincronizando ...
      uses: airvzxf/ftp-deployment-action@latest
      with:
        server: 15.229.86.2
        user: admin
        password: admin
